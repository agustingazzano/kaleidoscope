import { Typography } from '@mui/material'

export const Header = () => {
    return (
        <div style={{ minHeight: '140px' }}>
            <Typography variant="h4">Kaleidoscope docs</Typography>
        </div>
    )
}
